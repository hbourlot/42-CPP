#include <iostream>
#include <cmath>

int	main() {

	double x = 1.2;
	double y = 4;
	double z;

	// z =  pow(2, 4);
	// z = std::max(x, y);
	// z = std::min(x, y);
	// z = sqrt(9);
	// z = abs(-9);
	// z = round(x);
	// z = ceil(x);
	// z =  floor(x);

	std::cout << z;

	return 0;
}